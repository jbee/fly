package de.jbee.fly.probability;

public interface Lexer {

	// is it a
	// changeover ?
	//   is it also a
	//   terminator ? 

	//non-significant (usually whitespace) vs. significant (letters, digits etc.) 
}
